package org.example.thewitcher.view.interaction;

import javafx.scene.canvas.Canvas;
import javafx.scene.canvas.GraphicsContext;
import org.example.thewitcher.view.util.ViewportCalculator;

public class InnkeeperView extends GenericInteractionView {
    public InnkeeperView(GraphicsContext gc, Canvas canvas, ViewportCalculator viewport) {
        super(gc, canvas, viewport);
    }
}
